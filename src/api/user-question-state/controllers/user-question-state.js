'use strict'

/**
 *  user-question-state controller
 */

const { createCoreController } = require('@strapi/strapi').factories

module.exports = createCoreController('api::user-question-state.user-question-state')
